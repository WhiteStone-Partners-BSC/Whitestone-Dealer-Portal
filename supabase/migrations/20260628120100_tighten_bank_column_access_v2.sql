-- v2: table-level SELECT on dealers bypassed column REVOKE; restrict to safe columns only.

revoke select on public.dealers from authenticated, anon;

grant select (
  id,
  username,
  password,
  dealership_name,
  location,
  phone,
  email,
  active,
  is_admin,
  created_at,
  auth_id,
  address,
  city,
  state,
  zip,
  website,
  contact_first_name,
  contact_last_name,
  contact_title,
  boat_brands,
  service_volume,
  joined_at,
  stripe_customer_id,
  onboarding_completed,
  onboarding_steps,
  dealer_number,
  legal_business_name,
  dba_name,
  business_address,
  business_city,
  business_state,
  business_zip,
  business_phone,
  ein,
  brands_carried,
  dealer_contacts,
  ar_contact,
  ar_phone,
  ar_email,
  effective_date,
  agreement_signed_at,
  stripe_bank_account_id,
  ach_verified,
  dba,
  ar_contact_name,
  authorized_contacts,
  enrollment_status,
  enrollment_submitted_at,
  enrollment_effective_date,
  agreement_signed_by,
  slug,
  box_recipient_first_name,
  box_recipient_last_name,
  box_recipient_title,
  box_shipped_at,
  box_delivered_at,
  box_tracking_number,
  box_recipient_email,
  followup_email_sent_at,
  organization_id,
  docusign_envelope_id,
  docusign_envelope_status,
  active_until,
  must_set_password
) on public.dealers to authenticated, anon;
